before_install:
- sudo apt-get update
- sudo apt-get -y install texlive-full

sudo: false
addons:
  apt:
    packages:
      - texlive
      - texlive-latex-extra
script:
    - cd paper/ && make clean && make
